Main program:
Select profile : Guest or Create user or choose user (binary file)
    add user
    delete user
    search user
    display all users
    edit user
Read from config file if it exists and let the user know program is using previous choices

Dashboard options:
    add city (same choices)
    remove city
    search city
    display all saved city
    edit saved city

Guest user:
    Input city: 
    Choices:
        types of weather - multiple options seperated by commas:
            units, all units
        all function to display everything
Save in log text file
error handling for this
repeat

Settings menu: (config file in csv file)
    change from config:
    imperial/metric
    Log on/off (text file)
    
TODOS:
